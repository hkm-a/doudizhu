class_name Test{{GameNamePascal}} extends GdUnitTestSuite

## Example test — replace with tests for your game logic.
## TDD: write the test first, then implement until it passes.

func test_example() -> void:
	# TODO: Replace with a real test
	# Example: test a component's default values
	# var comp = C_Velocity.new()
	# assert_that(comp.x).is_equal(0.0)
	assert_that(true).is_true()
